<!DOCTYPE html>
<html lang="en-us">
<head>
  <meta charset="utf-8">
  <title>Cluster Rush + Web Osu Mania Menu</title>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      background: #111;
      font-family: sans-serif;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    #menu {
      background: rgba(34, 34, 34, 0.9);
      border: 1px solid #555;
      border-radius: 8px;
      padding: 20px;
      min-width: 300px;
      text-align: center;
    }

    #menu input {
      width: 90%;
      padding: 6px;
      border-radius: 4px;
      border: 1px solid #555;
      margin-bottom: 10px;
      background: #111;
      color: #fff;
    }

    #menu button {
      display: block;
      width: 90%;
      margin: 6px auto;
      padding: 8px 0;
      border: none;
      border-radius: 6px;
      background: #0f0;
      font-weight: bold;
      cursor: pointer;
    }
  </style>
</head>
<body>

  <div id="menu">
    <label>Redirect link:</label><br>
    <input type="text" id="linkInput" placeholder="https://example.com"><br>
    <button id="saveBtn">Save Link</button>
    <hr>
    <button id="osuBtn">🎵 Play Web Osu Mania</button>
    <button id="clusterBtn">🦖 Play Cluster Rush</button>
    <button id="openLinkBtn">➡ Go to Saved Link</button>
  </div>

  <script>
    const linkInput = document.getElementById("linkInput");
    const saveBtn = document.getElementById("saveBtn");
    const osuBtn = document.getElementById("osuBtn");
    const clusterBtn = document.getElementById("clusterBtn");
    const openLinkBtn = document.getElementById("openLinkBtn");

    // Load saved redirect link
    linkInput.value = localStorage.getItem("redirectLink") || "https://classroom.google.com/h";

    saveBtn.onclick = () => {
      const link = linkInput.value.trim();
      if (link) {
        localStorage.setItem("redirectLink", link);
        alert("✅ Link saved!");
      }
    };

    osuBtn.onclick = () => {
      window.open("https://kingnoobisw.github.io/WebOsuMania/", "_blank");
    };

    clusterBtn.onclick = () => {
      window.open("https://swordslasher.com/games/cluster-rush/game.html", "_blank");
    };

    openLinkBtn.onclick = () => {
      const link = localStorage.getItem("redirectLink") || "https://classroom.google.com/h";
      window.open(link, "_blank");
    };

    // Redirect key
    document.addEventListener("keydown", function(event) {
      if (event.key === ">") {
        const link = localStorage.getItem("redirectLink") || "https://classroom.google.com/h";
        window.open(link, "_blank");
      }
    });
  </script>

</body>
</html>

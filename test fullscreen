<!DOCTYPE html>
<html lang="en-us">
<head>
  <meta charset="utf-8">
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
  <title>Cluster Rush + Web Osu Mania Menu</title>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      overflow: hidden;
      height: 100%;
      background: #111;
      font-family: sans-serif;
    }

    .game-container {
      position: relative;
      width: 100vw;
      height: 100vh;
    }

    .game-container iframe {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      border: none;
      z-index: 1;
      pointer-events: auto; /* game receives clicks where menu isn’t */
    }

    #topButtons {
      position: absolute;
      top: 10px;
      left: 10px;
      z-index: 10;
      display: flex;
      gap: 10px;
      pointer-events: auto;
    }

    #topButtons button {
      background: #333;
      color: #fff;
      border: none;
      border-radius: 6px;
      padding: 6px 10px;
      cursor: pointer;
      font-weight: bold;
    }

    #menu {
      position: absolute;
      top: 60px;
      left: 10px;
      z-index: 11;
      background: rgba(34, 34, 34, 0.85); /* semi-transparent */
      border: 1px solid #555;
      border-radius: 6px;
      padding: 10px;
      display: none;
      pointer-events: auto;
      cursor: move; /* draggable hint */
    }

    #menu input {
      width: 220px;
      padding: 4px;
      border-radius: 4px;
      border: 1px solid #555;
      background: #111;
      color: #fff;
    }

    #menu button {
      margin-top: 6px;
      background: #0f0;
      border: none;
      padding: 5px 12px;
      border-radius: 4px;
      cursor: pointer;
      width: 100%;
    }
  </style>
</head>
<body>

  <!-- --- MENU + NEW TAB BUTTONS --- -->
  <div id="topButtons">
    <button id="menuBtn">☰ Menu</button>
    <button id="newTabBtn">↗ Open in New Tab</button>
  </div>

  <!-- --- DRAGGABLE MENU --- -->
  <div id="menu">
    <label>Redirect link:</label><br>
    <input type="text" id="linkInput" placeholder="https://example.com"><br>
    <button id="saveBtn">Save Link</button>
    <hr>
    <button id="osuBtn">🎵 Play Web Osu Mania</button>
    <button id="clusterBtn">🦖 Play Cluster Rush</button>
  </div>

  <!-- --- GAME CONTAINERS --- -->
  <div id="clusterRushContainer" class="game-container">
    <iframe src="https://swordslasher.com/games/cluster-rush/game.html" allowfullscreen></iframe>
  </div>

  <div id="osuContainer" class="game-container" style="display:none;">
    <iframe src="https://hectickiwi.github.io/Web-Osu-Mania/" allowfullscreen></iframe>
  </div>

  <script>
    const menuBtn = document.getElementById("menuBtn");
    const menu = document.getElementById("menu");
    const linkInput = document.getElementById("linkInput");
    const saveBtn = document.getElementById("saveBtn");
    const osuBtn = document.getElementById("osuBtn");
    const clusterBtn = document.getElementById("clusterBtn");
    const newTabBtn = document.getElementById("newTabBtn");

    const clusterRushContainer = document.getElementById("clusterRushContainer");
    const osuContainer = document.getElementById("osuContainer");

    // Load saved redirect link
    linkInput.value = localStorage.getItem("redirectLink") || "https://classroom.google.com/h";

    menuBtn.onclick = () => {
      menu.style.display = (menu.style.display === "none" || menu.style.display === "") ? "block" : "none";
    };

    saveBtn.onclick = () => {
      const link = linkInput.value.trim();
      if (link) {
        localStorage.setItem("redirectLink", link);
        alert("✅ Link saved! Press '>' to go there.");
        menu.style.display = "none";
      }
    };

    // Switch between games
    osuBtn.onclick = () => {
      clusterRushContainer.style.display = "none";
      osuContainer.style.display = "block";
      menu.style.display = "none";
    };

    clusterBtn.onclick = () => {
      osuContainer.style.display = "none";
      clusterRushContainer.style.display = "block";
      menu.style.display = "none";
    };

    // Open in new tab
    newTabBtn.onclick = () => {
      const isOsuVisible = osuContainer.style.display === "block";
      const url = isOsuVisible
        ? "https://hectickiwi.github.io/Web-Osu-Mania/"
        : "https://swordslasher.com/games/cluster-rush/game.html";
      const newWin = window.open(url, "_blank");
      if (!newWin) window.location.href = url;
    };

    // Redirect key
    document.addEventListener("keydown", function(event) {
      if (event.key === ">") {
        const link = localStorage.getItem("redirectLink") || "https://classroom.google.com/h";
        window.location.href = link;
      }
    });

    // --- Draggable menu ---
    let isDragging = false;
    let offsetX = 0;
    let offsetY = 0;

    menu.addEventListener('mousedown', function(e) {
      isDragging = true;
      offsetX = e.clientX - menu.offsetLeft;
      offsetY = e.clientY - menu.offsetTop;
      menu.style.cursor = 'grabbing';
    });

    document.addEventListener('mousemove', function(e) {
      if (isDragging) {
        let left = e.clientX - offsetX;
        let top = e.clientY - offsetY;

        // Keep menu inside viewport
        left = Math.max(0, Math.min(left, window.innerWidth - menu.offsetWidth));
        top = Math.max(0, Math.min(top, window.innerHeight - menu.offsetHeight));

        menu.style.left = left + 'px';
        menu.style.top = top + 'px';
      }
    });

    document.addEventListener('mouseup', function() {
      isDragging = false;
      menu.style.cursor = 'move';
    });
  </script>

</body>
</html>

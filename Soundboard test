<!DOCTYPE html>
<html lang="en-us">
<head>
  <meta charset="utf-8">
  <title>Game Menu</title>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      background: #111;
      font-family: sans-serif;
      height: 100vh;
      overflow: hidden;
    }

    iframe {
      position: absolute;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      border: none;
      z-index: 0;
    }

    #menu {
      position: absolute;
      top: 10px;
      left: 10px;
      z-index: 1000;
      background: rgba(34, 34, 34, 0.95);
      border: 1px solid #555;
      border-radius: 8px;
      padding: 10px;
      min-width: 220px;
    }

    #menu input {
      width: 90%;
      padding: 4px;
      margin-bottom: 6px;
      border-radius: 4px;
      border: 1px solid #555;
      background: #111;
      color: #fff;
    }

    #menu button {
      display: block;
      width: 90%;
      margin: 6px auto;
      padding: 6px 0;
      border: none;
      border-radius: 6px;
      background: #0f0;
      font-weight: bold;
      cursor: pointer;
    }
  </style>
</head>
<body>

  <!-- Menu -->
  <div id="menu">
    <label>Redirect link:</label><br>
    <input type="text" id="linkInput" placeholder="https://example.com"><br>
    <button id="saveBtn">Save Link</button>
    <hr>
    <button id="osuBtn">🎵 Play Web Osu Mania</button>
    <button id="clusterBtn">🦖 Play Cluster Rush</button>
    <button id="soundboardBtn">🔊 Play Soundboard</button>
  </div>

  <!-- Iframes for games -->
  <iframe id="gameFrame" src=""></iframe>

  <script>
    const linkInput = document.getElementById("linkInput");
    const saveBtn = document.getElementById("saveBtn");
    const osuBtn = document.getElementById("osuBtn");
    const clusterBtn = document.getElementById("clusterBtn");
    const soundboardBtn = document.getElementById("soundboardBtn");
    const gameFrame = document.getElementById("gameFrame");

    // Load saved redirect link
    linkInput.value = localStorage.getItem("redirectLink") || "https://classroom.google.com/h";

    saveBtn.onclick = () => {
      const link = linkInput.value.trim();
      if (link) {
        localStorage.setItem("redirectLink", link);
        alert("✅ Link saved!");
      }
    };

    // Load games in the same iframe
    osuBtn.onclick = () => {
      gameFrame.src = "https://kingnoobisw.github.io/WebOsuMania/";
    };

    clusterBtn.onclick = () => {
      gameFrame.src = "https://swordslasher.com/games/cluster-rush/game.html";
    };

    soundboardBtn.onclick = () => {
      gameFrame.src = "https://special-scarlet-yijlvuwhem.edgeone.app/";
    };

    // Redirect key
    document.addEventListener("keydown", function(event) {
      if (event.key === ">") {
        const link = localStorage.getItem("redirectLink") || "https://classroom.google.com/h";
        gameFrame.src = link;
      }
    });
  </script>

</body>
</html>

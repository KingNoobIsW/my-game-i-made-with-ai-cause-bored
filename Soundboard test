<!DOCTYPE html>
<html lang="en-us">
<head>
  <meta charset="utf-8">
  <title>Game Menu</title>
  <style>
    body {
      margin: 0;
      background: #111;
      overflow: hidden;
      font-family: sans-serif;
    }

    canvas {
      display: block;
      width: 100vw;
      height: 100vh;
      border: none;
    }

    #menuBtn {
      position: absolute;
      top: 10px;
      left: 10px;
      z-index: 10;
      background: #333;
      color: #fff;
      border: none;
      border-radius: 6px;
      padding: 6px 10px;
      cursor: pointer;
    }

    #menu {
      position: absolute;
      top: 45px;
      left: 10px;
      z-index: 10;
      background: #222;
      border: 1px solid #555;
      border-radius: 6px;
      padding: 10px;
      display: none;
    }

    #menu input {
      width: 220px;
      padding: 4px;
      border-radius: 4px;
      border: 1px solid #555;
      background: #111;
      color: #fff;
    }

    #menu button {
      margin-top: 6px;
      background: #0f0;
      border: none;
      padding: 5px 12px;
      border-radius: 4px;
      cursor: pointer;
      width: 100%;
    }
  </style>
</head>
<body>

  <!-- Menu Button + Dropdown -->
  <button id="menuBtn">☰ Menu</button>
  <div id="menu">
    <label>Redirect link:</label><br>
    <input type="text" id="linkInput" placeholder="https://example.com"><br>
    <button id="saveBtn">Save Link</button>
    <hr>
    <button id="osuBtn">🎵 Play Web Osu Mania</button>
    <button id="clusterBtn">🦖 Play Cluster Rush</button>
    <button id="soundboardBtn">🔊 Play Soundboard</button>
  </div>

  <script>
    const menuBtn = document.getElementById("menuBtn");
    const menu = document.getElementById("menu");
    const linkInput = document.getElementById("linkInput");
    const saveBtn = document.getElementById("saveBtn");
    const osuBtn = document.getElementById("osuBtn");
    const clusterBtn = document.getElementById("clusterBtn");
    const soundboardBtn = document.getElementById("soundboardBtn");

    // Load saved redirect link
    linkInput.value = localStorage.getItem("redirectLink") || "https://classroom.google.com/h";

    menuBtn.onclick = () => {
      menu.style.display = (menu.style.display === "none" || menu.style.display === "") ? "block" : "none";
    };

    saveBtn.onclick = () => {
      const link = linkInput.value.trim();
      if (link) {
        localStorage.setItem("redirectLink", link);
        alert("✅ Link saved! Press '>' to go there.");
        menu.style.display = "none";
      }
    };

    // Open games in the same tab
    osuBtn.onclick = () => {
      window.location.href = "https://kingnoobisw.github.io/WebOsuMania/";
    };

    clusterBtn.onclick = () => {
      window.location.href = "https://swordslasher.com/games/cluster-rush/game.html";
    };

    soundboardBtn.onclick = () => {
      window.location.href = "https://special-scarlet-yijlvuwhem.edgeone.app/";
    };

    // Redirect key
    document.addEventListener("keydown", function(event) {
      if (event.key === ">") {
        const link = localStorage.getItem("redirectLink") || "https://classroom.google.com/h";
        window.location.href = link;
      }
    });
  </script>

</body>
</html>
